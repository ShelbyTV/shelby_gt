defaults: &defaults
  db_options:
   :poolsize: 1

development:
  <<: *defaults

test:
  <<: *defaults

production: &production
  <<: *defaults
  db_options:
   :poolsize: 5
  
arnold:
  <<: *production
  db_options:
   :poolsize: 5