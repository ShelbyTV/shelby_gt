defaults: &defaults
  title: "Shelby.tv"
  domain: "shelby.tv"
  override_em_next_tick: false

development:
  <<: *defaults

test:
  <<: *defaults

production: &production
  <<: *defaults

arnold:
  <<: *production
  override_em_next_tick: true