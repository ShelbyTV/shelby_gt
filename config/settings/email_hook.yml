defaults: &defaults
  default_rolling_comment: 'Rolled via email'
  email_hook_domain: 'volumevolume.com'
  email_user_keys: {
      roll : 'roll'
  }

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults

arnold:
  <<: *defaults