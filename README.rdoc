== TODO

Make sure rake runs the tasks 'db:test:prepare', 'db:indexes:ensure' in TEST environment (current have to run `rake RAILS_ENV=test`)

Change the indexes to use abbreviations and tests should make sure it's all aligned