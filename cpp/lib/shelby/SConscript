
Import('*')

libraryName = 'shelby'
sourceFiles = Split("""
                       sob.c
                    """)

cppPath = ['#/source', '#']

shelbySimpleLibrary(libraryName, sourceFiles, cppPath, defaultDebugEnv, defaultOptEnv)

